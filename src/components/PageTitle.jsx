const PageTitle = () => {
  return (
    <div>
      <h1 className="text-3xl font-bold text-slate-800">
        User Management Dashboard
      </h1>

      <p className="mt-2 text-slate-500">
        Manage users with search, filtering, sorting and CRUD operations.
      </p>
    </div>
  );
};

export default PageTitle;